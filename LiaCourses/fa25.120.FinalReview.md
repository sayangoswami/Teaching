<!--
author:   Sayan Goswami
email:    sgoswami@smcm.edu
version:  0.1.0
language: en
narrator: US English Female

comment:  Final Exam review

link: https://fonts.googleapis.com/css2?family=Fira+Sans:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap

link: https://fonts.googleapis.com/css2?family=Fira+Code:wght@300..700&family=Fira+Sans:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap

link: https://cdn.jsdelivr.net/gh/sayangoswami/Teaching@main/LiaCourses/theme.css

-->

# COCS120 Final Exam Review Quiz

## Q1: Integer Division and Casting

**Topic:** Operators / Variables
**Question:**
What is the output of the following code snippet?

```java
int a = 5;
int b = 2;
double c = 3.0;
System.out.println((double)(a / b) + c);
```

**Options:**

  - [( )] 5.5
  - [(X)] 5.0
  - [( )] 2.5
  - [( )] Compilation Error

## Q2: Precedence and Modulus

**Topic:** Operators
**Question:**
Evaluate the result of `x` after this statement executes:

```java
int x = 10 + 4 % 3 * 2 - 1;
```

**Options:**

  - [( )] 13
  - [( )] 17
  - [(X)] 11
  - [( )] 1

## Q3: Short-Circuit Logic

**Topic:** Conditional Statements
**Question:**
Consider the following code. How many times is the `check()` method called?

```java
boolean check() {
    System.out.print("Run ");
    return false;
}

// Inside main:
if (check() && check()) {
    System.out.println("True");
} else {
    System.out.println("False");
}
```

**Options:**

  - [( )] 0
  - [(X)] 1
  - [( )] 2
  - [( )] 3

## Q4: Scope and Shadowing

**Topic:** Variables
**Question:**
What is the output?

```java
int x = 10;
if (x > 5) {
    int x = 20;
    System.out.print(x + " ");
}
System.out.print(x);
```

**Options:**

  - [( )] 10
  - [( )] 20
  - [( )] 0
  - [(x)] Error

## Q5: While Loop Logic

**Topic:** Loops
**Question:**
What will be printed by this loop?

```java
int i = 0;
while (i < 3) {
    System.out.print(i + " ");
    i++;
}
System.out.print(i);
```

**Options:**

  - [( )] 0 1 2 2
  - [( )] 0 1 2
  - [(X)] 0 1 2 3
  - [( )] 1 2 3 3

## Q6: Nested Loops

**Topic:** Loops
**Question:**
What is the final value of `count`?

```java
int count = 0;
for (int i = 0; i < 4; i++) {
    for (int j = i; j < 4; j++) {
        count++;
    }
}
```

**Options:**

  - [( )] 16
  - [( )] 12
  - [(X)] 10
  - [( )] 8

## Q7: Array Initialization

**Topic:** Arrays
**Question:**
Which of the following lines correctly initializes an array of integers with values 1, 2, and 3?
**Options:**

  - [( )] `int[] arr = new int[3] {1, 2, 3};`
  - [(X)] `int[] arr = {1, 2, 3};`
  - [( )] `int arr[3] = {1, 2, 3};`
  - [( )] `int[] arr = new int(1, 2, 3);`

## Q8: Array Indexing

**Topic:** Arrays
**Question:**
What happens when this code runs?

```java
int[] nums = {10, 20, 30};
System.out.println(nums[3]);
```

**Options:**

  - [( )] Prints 30
  - [( )] Prints 0
  - [( )] Prints garbage value
  - [(X)] Throws an ArrayIndexOutOfBoundsException

## Q9: Reference Copying

**Topic:** Arrays / Variables
**Question:**
What is printed?

```java
int[] a = {1, 2};
int[] b = a;
b[0] = 99;
System.out.println(a[0]);
```

**Options:**

  - [( )] 1
  - [(X)] 99
  - [( )] 0
  - [( )] Error

## Q10: Method Return Values

**Topic:** Methods
**Question:**
What does this method return if called with `calculate(5)`?

```java
int calculate(int n) {
    if (n > 10) return n;
    n = n * 2;
    return n + 1;
}
```

**Options:**

  - [( )] 5
  - [( )] 10
  - [(X)] 11
  - [( )] 0

## Q11: Pass by Value (Primitives)

**Topic:** Passing values/references
**Question:**
What is the output?

```java
void modify(int x) {
    x = 100;
}
// main:
int num = 5;
modify(num);
System.out.println(num);
```

**Options:**

  - [( )] 100
  - [(X)] 5
  - [( )] 0
  - [( )] Compilation Error

## Q12: Pass by Reference (Objects/Arrays)

**Topic:** Passing values/references
**Question:**
What is the output?

```java
void modify(int[] arr) {
    arr[0] = 100;
}
// main:
int[] nums = {1, 2, 3};
modify(nums);
System.out.println(nums[0]);
```

**Options:**

  - [(X)] 100
  - [( )] 1
  - [( )] 0
  - [( )] Compilation Error

## Q13: Basic Class Instantiation

**Topic:** Classes
**Question:**
Given the class `Dog`, which line creates an object correctly?

```java
class Dog {
    String name;
}
```

**Options:**

  - [( )] `Dog d = Dog();`
  - [( )] `Dog d;`
  - [(X)] `Dog d = new Dog();`
  - [( )] `new Dog d;`

## Q14: Default Constructor

**Topic:** Constructors
**Question:**
Does this code compile?

```java
class Box {
    int width;
    Box(int w) { width = w; }
}
// main:
Box b = new Box();
```

**Options:**

  - [( )] Yes, `width` is 0.
  - [(X)] No, the default constructor is undefined.
  - [( )] Yes, `width` is null.
  - [( )] No, you cannot define constructors with parameters.

## Q15: Constructor Logic

**Topic:** Constructors
**Question:**
What is the value of `x` inside the object created?

```java
class Test {
    int x;
    Test() {
        x = 5;
    }
    Test(int val) {
        x = val;
    }
}
// main:
Test t = new Test(10);
```

**Options:**

  - [( )] 5
  - [(X)] 10
  - [( )] 0
  - [( )] 15

## Q16: Static Variables

**Topic:** Static methods/variables
**Question:**
What is the output?

```java
class Counter {
    static int count = 0;
    Counter() { count++; }
}
// main:
new Counter();
new Counter();
System.out.println(Counter.count);
```

**Options:**

  - [( )] 0
  - [( )] 1
  - [(X)] 2
  - [( )] Compilation Error

## Q17: Static Methods

**Topic:** Static methods
**Question:**
Which statement is true regarding static methods?

```java
class Utility {
    int id;
    static void printInfo() {
        // Line A: 
        System.out.println("Hello");
        // Line B: 
        System.out.println(id);
    }
}
```

**Options:**

  - [( )] Both Line A and Line B are valid.
  - [(X)] Line A is valid; Line B causes a compile error.
  - [( )] Line B is valid; Line A causes a compile error.
  - [( )] Static methods cannot print to console.

## Q18: Aggregation vs Composition Code

**Topic:** Aggregation
**Question:**
In Aggregation, the contained object can exist independently of the container. Which code snippet best represents `Department` having an aggregation relationship with `Teacher`?
**Options:**

  - [( )] `class Dept { Teacher t = new Teacher(); }`
  - [(X)] `class Dept { Teacher t; Dept(Teacher t) { this.t = t; } }`
  - [( )] `class Dept { void method() { Teacher t = new Teacher(); } }`
  - [( )] `class Dept extends Teacher { }`

## Q19: Composition Semantics

**Topic:** Composition
**Question:**
In Composition, if the parent object is destroyed, the child object is also destroyed (or has no meaning alone). Which implementation of a `House` and `Room` suggests Composition?
**Options:**

  - [(X)] `class House { Room r; House() { r = new Room(); } }`
  - [( )] `class House { Room r; House(Room r) { this.r = r; } }`
  - [( )] `class House { void setRoom(Room r) { this.r = r; } }`
  - [( )] `class House { static Room r; }`

## Q20: Accessing Object Fields

**Topic:** Classes
**Question:**
Given:

```java
class Player {
    private int score;
    public void setScore(int s) { score = s; }
}
```

How do you legally set the score from another class?
**Options:**

  - [( )] `Player p = new Player(); p.score = 10;`
  - [(X)] `Player p = new Player(); p.setScore(10);`
  - [( )] `Player.score = 10;`
  - [( )] `Player p = new Player(); score = 10;`

## Q21: Method Invocation

**Topic:** Methods
**Question:**
What is the output?

```java
void foo() { System.out.print("A"); }
void bar() { System.out.print("B"); foo(); }
// main:
bar();
```

**Options:**

  - [( )] B
  - [( )] A
  - [(X)] BA
  - [( )] AB

## Q22: Conditional Logic (Boolean)

**Topic:** Conditional Statements
**Question:**
What is printed?

```java
boolean a = true;
boolean b = false;
if (a || b) {
    if (a && !b) System.out.println("X");
    else System.out.println("Y");
} else {
    System.out.println("Z");
}
```

**Options:**

  - [(X)] X
  - [( )] Y
  - [( )] Z
  - [( )] Nothing

## Q23: Array Length

**Topic:** Arrays
**Question:**
How do you get the number of elements in an array named `data`?
**Options:**

  - [( )] `data.length()`
  - [(X)] `data.length`
  - [( )] `data.size()`
  - [( )] `len(data)`

## Q24: Loop Control (Break)

**Topic:** Loops
**Question:**
What is the output?

```java
for (int i = 0; i < 5; i++) {
    if (i == 2) break;
    System.out.print(i);
}
```

**Options:**

  - [( )] 012
  - [(X)] 01
  - [( )] 01234
  - [( )] 0134

## Q25: Loop Control (Continue)

**Topic:** Loops
**Question:**
What is the output?

```java
for (int i = 0; i < 4; i++) {
    if (i == 1) continue;
    System.out.print(i);
}
```

**Options:**

  - [( )] 0123
  - [( )] 0
  - [(X)] 023
  - [( )] 02

## Q26: Reference Reassignment

**Topic:** Passing values/references
**Question:**
What is the output?

```java
void change(String s) {
    s = "World";
}
// main:
String str = "Hello";
change(str);
System.out.println(str);
```

**Options:**

  - [( )] World
  - [(X)] Hello
  - [( )] HelloWorld
  - [( )] Error

## Q27: Static Method Access

**Topic:** Static methods
**Question:**
How should you call a static method `run()` defined in class `Game` from the `main` method?
**Options:**

  - [( )] `new Game().run();` (Valid but not recommended)
  - [(X)] `Game.run();`
  - [( )] `Game g; g.run();`
  - [( )] `run();`

## Q28: Constructor Chaining (Concept)

**Topic:** Constructors
**Question:**
Inside a constructor of a class called `Game`, how do you call another constructor of the same class?
**Options:**

  - [( )] `super();`
  - [(X)] `this();`
  - [( )] `Game();`
  - [( )] You cannot call one constructor from another.

## Q29: Aggregation (Null Checks)

**Topic:** Aggregation
**Question:**
In an aggregation where `Car` has a `Driver`, what is a potential risk if `driver` is not initialized?

```java
class Car {
    Driver driver;
    String getDriverName() {
        return driver.name;
    }
}
```

**Options:**

  - [( )] Compilation Error
  - [( )] Logical Error (returns empty string)
  - [(X)] NullPointerException at runtime
  - [( )] Memory Leak

## Q30: Accessing Bounds
**Topic:** Exceptions
**Question:** What happens when you execute the following code?
```
int[] numbers = {1, 2, 3, 4, 5};
System.out.println(numbers[5]);
```

- [( )] It prints 5.
- [( )] It prints 0.
- [(x)] It throws an ArrayIndexOutOfBoundsException.
- [( )] It prints null.
